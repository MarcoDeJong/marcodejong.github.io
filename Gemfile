source "https://rubygems.org"

# Use the GitHub Pages gem to match the GitHub Pages environment locally.
gem "github-pages", group: :jekyll_plugins

# Required for `bundle exec jekyll serve` on Ruby 3+.
gem "webrick", "~> 1.8"
